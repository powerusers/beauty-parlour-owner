import React from "react";
import ReactDOM from "react-dom/client";
import OwnerDashboard from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <OwnerDashboard />
  </React.StrictMode>
);
